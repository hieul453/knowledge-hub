# Roadmap Index (Quick-Check Reference)

Update this file whenever a roadmap is added or extended. Purpose: fast overlap check before generating a new roadmap — open the actual file for detail.

## Git — Phase 1 (Team Collaboration)
Stages: 1 Mental model & setup (snapshot data model, 3 areas, init/clone) · 2 Daily loop (status/add/commit/diff/log, .gitignore) · 3 Branching & merging (branches, merge, conflicts, rebase basics) · 4 Undo & recovery (restore, amend, reset, revert, reflog, stash) · 5 Remotes & collaboration (remote, fetch/pull/push, basic PR workflow, GitHub flow) · 6 Pro/QAOps workflows (branching strategies, tags, CI triggers from Git events, bisect, hooks)
Out of scope: plumbing internals, history rewriting/filter-repo, submodules, GUI clients.

## GitHub — Phase 1 (The Platform)
Explicitly excludes Git engine mechanics (owned by the Git roadmap) — covers the platform layered on top.
Stages: 1 Platform & repos (repo creation/visibility, repo UI, GFM markdown, SSH/PAT auth, gh CLI) · 2 Collaboration surfaces (Issues/templates, issue↔PR linking, PR *review* UI, Discussions, CODEOWNERS) · 3 Planning (Projects table/board/roadmap, custom fields/iterations, milestones/labels, automations) · 4 Actions I (events/workflows/jobs/steps model, runners, YAML anatomy, first CI workflow as PR check) · 5 Actions II (secrets, GITHUB_TOKEN/permissions, caching, matrix builds, artifacts, wiring a real E2E suite into CI) · 6 Governance/security/releases (branch protection/rulesets, permission levels, Dependabot/secret scanning/CodeQL, releases/tags)
Out of scope: Git internals, org/enterprise admin, custom Actions authoring, REST/GraphQL/webhooks/Apps, Codespaces, Copilot, full CD.

## JavaScript — Phase 1 (QAOps + Full-Stack)
Stages: 1 Core fundamentals (vars, types, coercion, conditionals, loops, functions) · 2 Data/collections/functional (arrays, objects, map/filter/reduce, destructuring/spread, closures, `this`, classes) · 3 Async & runtime (event loop, callbacks→promises→async/await, fetch, error handling) · 4 Browser & DOM (DOM tree/selectors, events, forms) · 5 Tooling/modules/Node (npm, package.json, ES modules, Node basics) · 6 Unit testing fundamentals (test runner, AAA, Vitest/Jest, mocks/stubs, coverage) · 7 Playwright E2E + API automation (setup, locators/auto-wait, web-first assertions, actions, API testing, fixtures, POM, CI/parallelization intro) · 8 Capstone (Express API + frontend + layered suite + CI)
Out of scope: frontend frameworks, TS deep-dive (→ separate TS roadmap), perf/load/visual/mobile testing, advanced backend (→ now covered by SQL Phase 1 for the DB layer), algorithms.

## TypeScript — Phase 1
Assumes JS Phase 1 complete.
Stages: 1 Compiler & mental model (superset of JS, type erasure, tsc, tsconfig, strict, inference) · 2 Core type system (primitives, arrays/tuples, unions/literals, any/unknown/never, satisfies) · 3 Shaping data (function typing, interfaces, interface vs type, structural typing, classes/access modifiers) · 4 Generics (generic functions/interfaces, inference, constraints, Promise<T>/Chainable<T>) · 5 Narrowing & utility types (type guards, discriminated unions, Partial/Pick/Omit/Record/Awaited, strictNullChecks) · 6 Real tools (tsconfig depth, @types/.d.ts, **typing Cypress custom commands via declare global**, typing Express, tsc --noEmit in CI) · 7 Capstone (convert app to TS, shared types module, typed layered suite, CI type-check gate)
Out of scope: conditional/mapped/template-literal types, TS+React, decorators, monorepo/project refs, compiler internals.
⚠️ Cross-reference: Stage 6 covers TS+Cypress custom commands, which the Cypress roadmap lists as out-of-scope for itself — point back here instead of re-teaching it in a future Cypress phase.

## SQL & Relational Databases — Phase 1 (PostgreSQL)
Three drivers weighted evenly: app backend (Supabase/Postgres for Visual Life Archive), QAOps (test-data seed/verify), general fluency. No JS prereq — stands on its own.
Stages: 1 Relational model & setup (tables/rows/columns, declarative SQL, connect to Postgres) · 2 Querying (SELECT/WHERE/IN/BETWEEN/LIKE, ORDER BY/LIMIT/DISTINCT/NULL, clause execution order) · 3 Tables & normalization (CREATE TABLE, data types, constraints, 1NF/2NF/3NF via anomalies) · 4 Relationships & JOINs 🧠 (FK/referential integrity, ON DELETE options, one-to-many/many-to-many, INNER/LEFT/FULL JOIN, Cartesian trap, anti-join pattern) · 5 Aggregation & subqueries (GROUP BY, HAVING vs WHERE, subqueries, CTEs) · 6 Writing data & transactions (INSERT/UPDATE/DELETE/upsert, RETURNING, BEGIN/COMMIT/ROLLBACK, ACID → test setup/teardown) · 7 Supabase & QAOps (Table Editor vs SQL Editor, RLS awareness only, seed/verify test state, index intro, EXPLAIN)
Out of scope: RLS in depth (**now covered — see Supabase Phase 1**), query performance tuning, window functions/recursive CTEs, PL/pgSQL, migrations tooling depth, ORMs, DB administration, NoSQL comparison.
⚠️ Cross-reference: Stage 7's light RLS awareness and its out-of-scope RLS deferral are now fulfilled/superseded by **Supabase Phase 1** (Stage 3). Stage 7 also designs the schema that **React Native + Expo Phase 1** (Stage 5) reads from and writes to.

## Supabase — Phase 1 (The Platform)
Three drivers weighted evenly: app backend, general BaaS fluency, QAOps. Self-contained (includes light SQL recall in Stages 1–2) but SQL Phase 1 remains the deeper source for the engine itself.
Stages: 1 Platform & setup (dashboard tour, anon/publishable vs service_role keys) · 2 Tables & relationships in Supabase (data types, identity vs uuid PKs, one-to-many/many-to-many — light SQL recall) · 3 Row Level Security 🧠 (deny-by-default, SELECT/INSERT/UPDATE/DELETE policies, USING vs WITH CHECK, auth.uid()/auth.jwt(), anon/authenticated/service_role + TO clause, ownership pattern, multi-tenant awareness) · 4 Authentication in depth (email/password, OAuth/magic-link/OTP, getSession vs getUser, onAuthStateChange, profiles table via trigger, SSR/mobile-storage-adapter awareness, MFA awareness) · 5 Storage (buckets public/private, upload/download, RLS on storage.objects, public vs signed URLs, image transforms) · 6 Realtime & Edge Functions (Postgres Changes/Broadcast/Presence, RLS-only-if-configured caveat, Deno/TS Edge Functions, secrets) · 7 Local dev, CLI & migrations 🧠 (CLI + local Docker stack, migration files, db diff, declarative schema, db reset + seed.sql, link/push, env/secrets across environments, gen types awareness) · 8 QAOps: testing (pgTAP vs application-level via supabase-js, testing RLS across anon/owner/other-user cases, seeding with service_role, supabase test db in CI)
Out of scope: deep Postgres/SQL engine (→ SQL Phase 1), advanced RLS at scale (RBAC, EXPLAIN tuning), self-hosting, pgvector/AI, advanced Postgres extensions, production ops at scale, framework-specific SSR auth deep-dives, management API/CI for the platform itself.
⚠️ Cross-reference: Stage 3 fulfills the "RLS in depth" deferral that **SQL Phase 1**'s Stage 7 and out-of-scope note pointed here — supersedes that note. Stages 5–6 (Auth/Storage/Realtime/Edge Functions) are the deeper platform source that **supersedes React Native + Expo Phase 1**'s Stage 5–6 Supabase intro on depth; the RN roadmap remains the "applied in a mobile client" consumer. Stage 8's pgTAP/CI work runs via **GitHub Phase 1** Stage 5 (Actions II).

## React (core concepts) — Phase 1
Prep track for React Native (Visual Life Archive app). Assumes JS Stages 1–3 + ES6.
Stages: 1 What React is & JSX (declarative UI, components, JSX, Vite scratch project) · 2 Components & props (props/one-way flow, composition, list rendering + key, conditional rendering) · 3 State & interactivity (useState, render loop, events, controlled inputs, state-as-snapshot) · 4 Side effects & data (useEffect + deps, fetching, loading/error/success states, cleanup) · 5 Thinking in React (one-way data flow, lifting state up, component-tree decomposition, useRef/useContext intro)
Out of scope: React Native itself (own roadmap), routing/Next.js/Server Components, state-mgmt libraries, perf hooks, TS+React, custom hooks/useReducer.
Note: styling/layout not covered here — see the Flexbox roadmap for the layout layer the React/RN frontend relies on.

## Flexbox (Layout & Styling) — Phase 1
Companion to the React / React Native frontend (Visual Life Archive app); taught universally (web CSS + React Native). Can be started in a browser before React — only Stage 4 leans on React.
Stages: 1 Mental model (flex container vs items, main vs cross axis, flex-direction, RN column-default preview) · 2 Aligning/distributing (justify-content, align-items, align-self, perfect centering) · 3 Sizing/flexibility (flex-grow/shrink/basis, flex shorthand + flex:1, flex-wrap, gap, align-content) · 4 Real layouts & React Native differences (header/content/footer column, wrapping card gallery, RN camelCase style objects/unitless dimensions/flexbox-only layout, aspect-ratio)
Out of scope: CSS Grid (separate topic; absent in React Native), rest of CSS (box model/positioning/animations/media queries), NativeWind/Tailwind/styled-components, responsive web beyond wrapping, RN styling beyond layout (theming/Dimensions/safe areas).

## React Native + Expo — Phase 1
Capstone for the Visual Life Archive app. Hard prereqs: React (core concepts) Phase 1, JS Stages 1–3. Written in JavaScript (TS retrofit deferred to TypeScript Phase 1). Flexbox Phase 1 Stage 4 is the RN layout prerequisite context.
Stages: 1 Mental model & first screen (create-expo-app, project structure, View/Text/Image/ScrollView vs web, New Architecture + EAS awareness) · 2 Core components, styling & Flexbox (StyleSheet.create, camelCase/dp units, flexDirection defaults, Pressable/TouchableOpacity/Button, controlled TextInput, safe areas) · 3 Lists & navigation (FlatList data/renderItem/keyExtractor/numColumns for a grid, Expo Router file-based routing/_layout/Stack vs Tabs/Link/dynamic [id] routes) · 4 Device capabilities (Expo permissions model, expo-image-picker library-pick + camera capture, asset URI, expo-image caching, AsyncStorage vs expo-secure-store) · 5 The backend: Supabase auth/DB/storage (supabase-js client + storage adapter + EXPO_PUBLIC_ env vars, email/password auth, entries table CRUD via supabase.from, RLS scoped to auth.uid(), Storage upload from device URI) · 6 Assembling the app (auth context + Expo Router protected routes, full capture→upload→save→render loop, loading/error/empty states, app.json/icon/splash, pull-to-refresh, .env hygiene)
Out of scope: TypeScript with RN (→ TypeScript Phase 1), store shipping (EAS Build & Submit), testing the RN app (Detox/Maestro/RNTL — candidate QAOps Phase 2), animations/gestures (Reanimated/Gesture Handler), push notifications/deep linking/social OAuth, Realtime & offline sync.
⚠️ Cross-reference: Stage 5's Supabase intro is superseded on depth by **Supabase Phase 1** (Stages 3–6) — this roadmap stays the "applied in a mobile client" layer.

## Cypress — Phase 1
Prereqs: JS async/DOM/HTTP/Node/testing-concepts fundamentals.
Stages: 1 Architecture & first test (in-browser model vs Selenium, install, open vs run mode, describe/it/visit/get) · 2 Core commands (get/contains, data-cy selectors, interactions, assertions/Chai) · 3 Async model & retry-ability [deep] (command queue, not-Promises, retry rules, aliases vs variables, .then()) · 4 Network & test data (cy.intercept spy/stub, fixtures, cy.request, cy.session) · 5 Structuring a real suite (custom commands, test isolation, Page Object vs App Actions, component testing intro) · 6 QAOps: CI/parallelization/limits (headless run, GitHub Action, parallelization, Cypress's limits vs Playwright)
Out of scope: TS+Cypress (**now partly covered — see TypeScript Stage 6**), visual regression/a11y, BDD/Cucumber, plugin authoring, Playwright itself.

---

**Shared prerequisite recall (expected, not wasted duplication):** async/promises, npm/Node basics, DOM/selectors, and CI fundamentals appear as brief prerequisite context across Git, JS, TS, SQL, Supabase, React, React Native + Expo, Cypress, and GitHub. That's intentional light overlap for grounding, not a repeated stage.